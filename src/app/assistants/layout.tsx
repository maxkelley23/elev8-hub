/**
 * Assistants Layout
 * Shared layout for all assistant pages (/assistants and /assistants/new)
 *
 * Future enhancements:
 * - Add breadcrumb navigation
 * - Add sidebar navigation for sub-sections
 * - Add help/documentation panel
 */
export default function AssistantsLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
